**From:** Monolith
**To:** Syndicate
**Date:** 18-03-2026
**Related Tickets:** CMD-01 (Monolith), CMD-03 (Syndicate), CMD-05 (Syndicate)

## What Was Done

Multi-tenant foundation migration script deployed. All 14 data tables now have `workspace_id UUID NOT NULL` with FK constraints and indexes.

### Tables Modified (workspace_id added)

| # | Table | FK Constraint Name | Index Name |
|---|-------|--------------------|------------|
| 1 | hub.projects | fk_projects_workspace | idx_projects_workspace_id |
| 2 | hub.phases | fk_phases_workspace | idx_phases_workspace_id |
| 3 | hub.teams | fk_teams_workspace | idx_teams_workspace_id |
| 4 | hub.tickets | fk_tickets_workspace | idx_tickets_workspace_id |
| 5 | hub.ticket_dependencies | fk_ticket_dependencies_workspace | idx_ticket_dependencies_workspace_id |
| 6 | hub.ticket_status_history | fk_ticket_status_history_workspace | idx_ticket_status_history_workspace_id |
| 7 | hub.roundtable_sessions | fk_roundtable_sessions_workspace | idx_roundtable_sessions_workspace_id |
| 8 | hub.team_chat_entries | fk_team_chat_entries_workspace | idx_team_chat_entries_workspace_id |
| 9 | hub.overseer_reports | fk_overseer_reports_workspace | idx_overseer_reports_workspace_id |
| 10 | hub.briefings | fk_briefings_workspace | idx_briefings_workspace_id |
| 11 | hub.decisions | fk_decisions_workspace | idx_decisions_workspace_id |
| 12 | hub.knowledge_items | fk_knowledge_items_workspace | idx_knowledge_items_workspace_id |
| 13 | hub.phase_approvals | fk_phase_approvals_workspace | idx_phase_approvals_workspace_id |
| 14 | hub.import_runs | fk_import_runs_workspace | idx_import_runs_workspace_id |

### New Tables Created

| Table | PK | Key Columns |
|-------|----|-------------|
| hub.workspaces | id UUID (gen_random_uuid()) | name, slug (unique), owner_user_id (FK users), plan CHECK(personal/team/enterprise), created_at, suspended_at |
| hub.workspace_members | (workspace_id, user_id) composite | role CHECK(owner/manager/contributor/viewer), joined_at, invited_by (FK users) |

### Column Definition

All 14 tables received: `workspace_id UUID NOT NULL REFERENCES hub.workspaces(id) ON DELETE CASCADE`

### Default Workspace

- ID: `00000000-0000-0000-0000-000000000001`
- Name: "Personal"
- Slug: "personal"
- Plan: "personal"
- All existing data backfilled to this workspace.

### Migration File

`deploy/init-scripts/32_multi_tenant_foundation.sql`

Note: Script numbered 32 (not 23 as in ticket notes) because existing scripts go up to 31. The migration must run AFTER all table-creation scripts.

### RLS-Ready Confirmation

All workspace_id columns are in place. CMD-02 (RLS policies) can now enable ROW LEVEL SECURITY on all 14 tables using `workspace_id = current_setting('app.workspace_id', true)::uuid`.

## What Remains

For Syndicate:
- **CMD-03 (JWT Workspace Middleware):** workspace_id columns exist. Middleware can now inject `app.workspace_id` session variable.
- **CMD-05 (Workspace API):** hub.workspaces and hub.workspace_members tables exist. WorkspacesController can now perform CRUD operations.

## Files Modified

| File | What Changed |
|------|-------------|
| deploy/init-scripts/32_multi_tenant_foundation.sql | NEW -- complete multi-tenant migration |

## Blockers / Risks

None. Migration is fully idempotent (safe to re-run).

## Notes

- The workspace_id on all tables is UUID type, matching the workspaces PK.
- ON DELETE CASCADE means deleting a workspace removes all associated data.
- The well-known default UUID (`00000000-...0001`) enables deterministic backfill on re-runs.
