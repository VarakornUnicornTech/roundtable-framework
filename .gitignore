# OS files
.DS_Store
Thumbs.db
desktop.ini

# IDE files
.vscode/
.idea/
*.swp
*.swo

# Local settings (user-specific, not part of template)
.claude/settings.local.json

# Project-specific data (generated at runtime, not part of template)
RoundTable/
Development/
Claude Projects/

# Claude Code memory (private per-user)
memory/

# Sync secrets (local only -- never commit)
secrets.local

# Backup files from /template-apply
*.backup
template-version.backup.json
